# Change Requests — Eternal Fitness Website

Per-project CR register (DO-SDLC Pipeline v1 / SOP-008). Git-tracked for the
first time 2026-08-17 — an untracked predecessor existed in the shared checkout
but was never committed (see the resolved process-gap note below). CR-EF-001–008
seeded from the SEO/AI-SEO/speed/spam audit run 2026-08-17; CR-EF-009–010 from a
concurrent hub-screens session the same day; CR-EF-011–015 reconciled from the
untracked predecessor; CR-EF-016–027 raised by Craig 2026-08-17 (hub/PWA
usability pass — see the grouping note under the table); CR-EF-028 recovered
2026-08-17 after an ID collision with CR-EF-016 (see the ID-collision note
near the bottom of this file).
CR-EF-029–041 recovered 2026-08-18 — raised 2026-08-17 on session
close but only ever committed to the unmerged branch
`claude/blocks-sessions-workouts-mobile-64d18f` and an untracked Work Order
doc; this register (on `main`) had silently stopped at CR-EF-028 while those
13 rows were cited by later commits and briefs that never existed here. See
the register-fork note under the table.

Status flow: raised → approved → briefed → built → verified.

**Bug vs CR boundary (per operating model):** the two items below marked `[BUG]`
are functional defects (something broken, not something new/improved) and belong
in the hub's task tracker (`decoded-ops-hub`, `task_type=bug`) once this project
has a live project_id there — not tracked yet, see note at bottom. Everything
else is a genuine change/improvement and lives here as a CR.

| ID | Item | Type | Status | Raised | Notes |
|---|---|---|---|---|---|
| CR-EF-001 | Re-enable Next.js image optimization (`images.unoptimized: false` + `sharp`) and compress oversized source images in `public/images/` (several 6–18MB originals) | CR (perf) | built | 2026-08-17 | Craig ran `pnpm add sharp` himself (gate has no self-service unblock). Config flipped, 17 source images compressed 108MB→30MB, verified live via dev server (`/_next/image` returning 200s with correct per-viewport widths, no console/server errors). Pushed `9a6fe2b`. |
| CR-EF-002 | [BUG] Contact form (`/api/leads`) has no honeypot/bot-trap — IP rate-limiting (added 2026-08-15) doesn't stop bots rotating IPs | Bug | built | 2026-08-17 | Honeypot field added to `app/contact/ContactPageClient.tsx` + server-side check in `app/api/leads/route.ts`. Not yet mirrored into the hub's live bug tracker (`decoded-ops-hub`, `task_type=bug`) — no confirmed project_id/DB access this session. |
| CR-EF-003 | `app/sitemap.ts` missing `/specialist-training` (live, indexable, nav-linked) | CR (SEO) | built | 2026-08-17 | Added. Comment updated to reflect the current re-gate (blog/cancer-rehab/falls-prevention excluded pending sign-off, not launch-scope). |
| CR-EF-004 | `/contact` title tag 80 chars, duplicates brand name — will be truncated in SERPs | CR (SEO) | built | 2026-08-17 | Fixed, `app/contact/page.tsx:21`. |
| CR-EF-005 | Meta descriptions on `/`, `/personal-training`, `/visual-impairment` run 170–197 chars, past Google's ~155–160 char practical limit | CR (SEO) | built | 2026-08-17 | Trimmed to 141–143 chars, keywords preserved. |
| CR-EF-006 | Add `Review`/`AggregateRating` schema to `/testimonials` (real named/dated quotes + "5.0 from 26 reviews" already in the HTML, no schema) | CR (SEO) | closed — not viable | 2026-08-18 | `wo defer` id `dmsx1yz4wkp`. Researched per its own deferral: Google's review-snippet guidance (Search Central, updated 2026-07-24) makes this content shape explicitly ineligible — "reviews about entity A placed on entity A's own site" (self-reviewed) and "aggregated from a third-party widget/Google Business reviews" are both named disqualifiers, and Eternal Fitness's testimonials hit both. Shipping the schema risks a manual action, not just a missed rich result — not implemented. Bonus find: `components/TestimonialsSection.tsx` (confirmed dead code, zero imports anywhere) already carried exactly this non-compliant pattern, including fabricated per-review 5-star `reviewRating` values with no source data behind them — deleted rather than left as a latent risk if ever re-wired in. |
| CR-EF-007 | Update `public/llms.txt` link from `/exercise-for-health` to `/specialist-training` directly (currently an extra redirect hop) | CR (AI-SEO) | built | 2026-08-17 | Fixed. |
| CR-EF-008 | HTTP→HTTPS redirect is 302 not 301; no HSTS header | CR (infra) | deferred | 2026-08-17 | `wo defer` id `dmsx1yz8hwt`. Proxy-level (Coolify/Traefik), not app code — out of this repo's scope. |
| CR-EF-009 | Design-parity catch-up for 4 unmocked hub screens (client intake, per-client documents, updates history/composer, block review) | CR (design parity) | built | 2026-08-17 | Merged to main+staging (commits 4f9330e..27cbb28). See `.context/audit-hub-mockup-reconciliation-2026-08-15.md` and `D:\apps\design-systems\ef-control-hub\brief-hub-remaining-screens-opendesign.md`. Renumbered from a since-superseded CR-EF-006 — see process-gap note below. |
| CR-EF-010 | Monthly calendar view for `/hub/schedule` (Outlook-style month grid alongside the existing day view) | CR (functionality) | built | 2026-08-17 | Per `hub-schedule-month.html` (Open Design). Read-only baseline; day view keeps booking create/edit/cancel. Renumbered from a since-superseded CR-EF-007. |
| CR-EF-011 | Consolidate workout/logging surfaces (Session Editor, Train Screen + Edit Sheet, Portal Training) to two designs — one desktop, one mobile PWA | CR (design + functionality) | approved | 2026-08-15 | Reconciled 2026-08-17 from the never-committed series (was CR-EF-001 there). Craig 2026-08-15: fold the standalone desktop Live Log (`/hub/log/[sessionId]`) into the Session Editor, its calendar/block "Log" links repoint there, route retired with a redirect. FF: amend EF DO-FF-001 training/logging rows (regenerate the doc, don't hand-edit HTML). Design: two Open Design briefs (desktop + PWA workout surface) — pending. Execution: `wo-ef-workout-consolidation-pwa-2026-08-15` (currently PLANNED). |
| CR-EF-012 | PWA installability — staff `/hub/m` prompt + portal PWA (own manifest/SW/offline queue) | CR (functionality) | approved, partially built | 2026-08-15 | Reconciled 2026-08-17 (was CR-EF-002). `/hub/m` PWA complete and real-device-verified 2026-08-17 (Craig confirmed install + offline logging works). Portal PWA still not built — no manifest/service worker found under `app/portal/` as of 2026-08-17. Same WO as CR-EF-011. |
| CR-EF-013 | Stale "Level 4 qualified trainer" framing in `public/site.webmanifest` | CR (content, small) | built | 2026-08-15 | Reconciled 2026-08-17 (was CR-EF-003). Fixed same day: manifest description now reads "...Esther Fair, qualified in Exercise Referral and Level 4 Cancer and Exercise Rehabilitation" — matches the phrasing already used in `app/about/page.tsx`'s schema.org blocks. The wider "Level 4 Personal Trainer" retirement (2026-07-27) had fixed visible copy + 3 schema.org blocks but missed this file. |
| CR-EF-014 | Pull-up/resistance bands prescribed and logged by colour (colour defines tension per the studio's band set), not just weight | CR (functionality) | approved | 2026-08-15 | Reconciled 2026-08-17 (was CR-EF-004). Rides the same WO/mockups as CR-EF-011 — band-colour selector is part of both design briefs, not a separate pass. Colour→tension mapping still needs confirming with Esther. FF-001 §M12 row · both Open Design briefs · WO unit. |
| CR-EF-015 | Workout-from-template on training blocks (RETRO-CAPTURED) | CR (functionality) | built, unverified against consolidation design | 2026-08-15 | Reconciled 2026-08-17 (was CR-EF-005). Built and pushed to main (094d35f) by a session that predated CR-register adoption. Flag carried forward: this added functionality to the Session Editor family while CR-EF-011's consolidation redesign is still pending mockups — those briefs MUST account for it so the redesign doesn't regress it. |
| CR-EF-016 | Client list should default to alphabetical (A–Z) order, not newest-first | CR (usability, S) | built | 2026-08-17 | Verified: `app/hub/(protected)/clients/page.tsx:13` ordered `created_at` descending. Mobile `/hub/m/clients` was already alphabetical (`page.tsx:96,181`) — desktop was the only gap. Fixed: query now orders by `name` ascending; the table's existing click-to-sort (`clients-table.tsx:50-51`) is unaffected. Verified live via the hub (A–Z from Amanda through Steph White). |
| CR-EF-017 | Notes section on the client profile front page in the mobile PWA — capture "spoke to X" / free-text notes against a client, timestamped | CR (functionality) | built | 2026-08-17 | Verified: `clients` has **no** notes column in any migration. The only note stores are `trainerize_client_notes` (imported historical, read-only) and `blocks.block_note` (per-block). Needs a new timestamped `client_notes` table + surfacing on `/hub/m/clients/[id]` (front page, above the fold) and on the desktop client page. Schema + Open Design brief before build. **Re-checked 2026-08-17 after the CR-EF-024 correction** (see accuracy note below) — no existing notes module was missed. The one near-miss: `client_tracker.notes` (`20260519_medical_clearance_tracker.sql`) is a single free-text blob on a **legacy, name-keyed** table that no app or component code references at all. Do not reuse it — it has no `client_id` and no per-entry timestamp, which is the whole point of this CR. **Corrected to built, 2026-08-18** — delivered in `4191575` (2026-08-17): `client_notes` table (migration `20260817_client_notes.sql`, applied to prod), `GET/POST /api/client-notes` + `DELETE /api/client-notes/[id]`, `NotesPanel` on the PWA profile, `ClientNotesPanel` on desktop. Confirmed present on this branch. |
| CR-EF-018 | Single-sided (unilateral) exercises must always be prescribed and logged per side — Left / Right | CR (functionality) | raised | 2026-08-17 | **Scope settled (Craig, 2026-08-17, `qmsxbh95t0x`): always-on for a known unilateral list.** Not a per-exercise flag Esther maintains, and not a per-prescription toggle — a movement on the unilateral list *always* splits L/R, with no way to turn it off per prescription. Implies a curated list is the first deliverable: derive a candidate set from the `exercises` table (the Trainerize-seeded library) and have Esther confirm it, since a wrong list silently changes how sets are prescribed. Touches `lib/prescription.ts`, the Session Editor, the PWA Train Screen, and the `set_logs` shape (a set becomes two sided rows — check the existing logging/history readers don't double-count volume). Rides CR-EF-011's mockups. |
| CR-EF-019 | Audible alert when the rest timer reaches zero | CR (functionality, S) | built | 2026-08-17 | Verified: there is **no** audio anywhere in the app — no `new Audio`, `AudioContext`, or `navigator.vibrate` in `app/hub`, `components/hub` or `lib`. The rest timer (`app/hub/m/train/[sessionId]/TrainScreen.tsx:294-317`) is visual only. Implementation note: iOS PWA blocks un-gestured audio, so the sound element must be primed on the user's "start rest" tap; add a `navigator.vibrate` fallback for silent mode. **Corrected to built, 2026-08-18** — delivered in `fb895df` (2026-08-17): AudioContext beep primed on the "start rest" tap, `navigator.vibrate` silent-mode fallback, `alertedRef` guards one alert per countdown. Confirmed present on this branch. |
| CR-EF-020 | Option to change rest times | CR (functionality) | built | 2026-08-17 | Verified: rest length is derived from the prescription string via `parseRestSeconds()` and falls back to a hard-coded `60` (`TrainScreen.tsx:990`, `:1004`). There is no in-session override, no per-exercise default and no per-client default. Scope to settle: adjust-for-this-set-only vs. edit the prescription. **Corrected to built, 2026-08-18** — delivered in `fb895df` (2026-08-17): +/-15s steppers on the upcoming rest (remembered per-exercise, floored at 5s) and +15s/-15s on the running countdown. Confirmed present on this branch. |
| CR-EF-021 | Edit button against each exercise inside session logging | CR (usability) | built | 2026-08-17 | Verified: the only edit path today is the whole-session sheet at `/hub/m/train/[sessionId]/edit` — nothing per-exercise inside the logging flow. Same surface as CR-EF-011's consolidation, so it belongs in those mockups rather than being bolted on first. **Corrected to built, 2026-08-18** — delivered in `4191575` (2026-08-17): pencil button on each exercise card in the Train Screen linking into the existing edit sheet. Craig's note in the same commit: functional-first, designs still in progress with the client. Confirmed present on this branch. |
| CR-EF-022 | Client account start date shown against the profile | CR (functionality, S) | built | 2026-08-17 | Verified: `clients` has **no** start-date column — the nearest existing fields are `block_expiry_date` and `trainerize_training_blocks.start_date` (imported history only). Needs a column, an intake-form field, backfill for existing clients, and display on both the desktop and PWA profile. **Corrected to built, 2026-08-18** — delivered in `4191575` (2026-08-17): `clients.start_date` DATE column (migration `20260817_client_start_date.sql`, applied to prod, nullable so existing clients stay blank until backfilled), field on the client edit form, shown on both desktop Snapshot and PWA profile Contact panel. Confirmed present on this branch. |
| CR-EF-023 | Update interval — allow an explicit date, or an arbitrary number of weeks | CR (functionality) | built | 2026-08-17 | Verified: `lib/updates-due.ts:9` was a fixed enum (`4_week` / `6_week` / `12_week` / `6_session` / `flexible`) with `INTERVAL_DAYS` = 28/42/84. Built: two new interval modes, `custom_weeks` (N weeks from last send, same recurring pattern as the presets) and `fixed_date` (a date Esther pins directly, doesn't auto-advance). Code changes: `lib/updates-due.ts`, `lib/updates-due-db.ts` (query widened + `LEFT JOIN`), `UpdateIntervalControl.tsx` (weeks/date inputs shown conditionally), `page.tsx`, reminder email shows the actual week count. `supabase/migrations/20260817_update_interval_custom.sql` **run against prod 2026-08-17** with Craig's go-ahead, via `scripts/run-update-interval-migration.mjs` (transaction-wrapped, before/after schema checked, committed clean — `update_interval_weeks`/`update_interval_next_date` columns added, CHECK constraint widened, client row count unchanged at 21). Both new dropdown options confirmed live in the hub. |
| CR-EF-024 | Quick-action task on the client page — **PWA only; the desktop already has it** | CR (functionality, S) | built | 2026-08-17 | **Scope settled (Craig, 2026-08-17, `qmsxbh96xbu`): EF-local tasks only — no `decoded-ops-hub` mirror.** **Correction, same day (Craig):** the hub already has a full tasks module — this CR originally and wrongly proposed building one. What exists: `tasks` table (`20260725_hub_tasks.sql`) with status/assignee/due_date, `task_buckets`, a `client_id` FK added `20260801_hub_tasks_client_link.sql`, `GET/POST /api/tasks` with `?client_id=` filtering, a kanban at `/hub/tasks`, and `ClientTasksPanel.tsx` **already rendered on the desktop client page** (`clients/[id]/page.tsx:10`) with an inline "Add task" quick-add (title field, Enter to save, `:205-244`). The genuine gap is the **mobile PWA**: tasks appear on the Today screen (`app/hub/m/page.tsx:77-87`) but the PWA client profile (`app/hub/m/clients/[id]`) has no tasks panel and no quick-add at all. So this is reuse + one PWA surface, not new schema. Pairs with CR-EF-017 (notes) — same screen, same one-tap-capture intent, design together. **Corrected to built, 2026-08-18** — delivered in `bb927e1` (2026-08-17): mobile `TasksPanel` (list + status-toggle + quick-add + delete) reusing the existing `/api/tasks` endpoints, rendered on `/hub/m/clients/[id]`. No schema change needed. Confirmed present on this branch. |
| CR-EF-025 | Client status | — | **closed — not wanted** | 2026-08-17 | Craig, 2026-08-17 (`qmsxbh982vf`): ignore. No work to do. Recorded rather than deleted so it isn't re-raised: `clients.client_status` exists in the schema (`active` / `inactive` / `completed` / `suspended` / `archived`) and remains deliberately unsurfaced — the client list's visible status filter is `compliance_status` (`clients-table.tsx:12-17`), which is the one that matters operationally. |
| CR-EF-026 | [BUG] Colin and Saffron show PAR-Q not signed, but their PAR-Qs are signed | Bug | built | 2026-08-17 | Root-caused as two different bugs with the same symptom (diagnosed live against production data via the hub, not `psql`; `lib/compliance.ts:31-38`). **Colin (#18):** compliance was quietly satisfied by a **legacy `signed_parq` row**, invisible on the Document Register, which only ever listed `client_documents` rows — his one visible PAR-Q line read "Sent," reasonably misread as unsigned. **Fixed:** `page.tsx` now builds a synthetic read-only "Legacy record" row (`legacyDocumentRows`) whenever compliance is satisfied via `latestParq`/`latestAgreement` with no matching signed `client_documents` row, and `DocumentRegister.tsx` renders it with no View/Open/delete actions (nothing to open) and a distinct "Legacy record" pill. Verified live: Colin's register now shows the legacy PAR-Q alongside the still-"Sent" one. **Saffron (#12):** a stale, never-completed "Sent" PAR-Q sat next to her real signed scanned original with no relationship shown, unlike Client Feedback documents on the same page which do get marked `Superseded`. **Fixed:** both places a document can become `signed` — `app/api/documents/upload/route.ts` (scanned uploads are always created signed) and `app/api/documents/[id]/sign/route.ts` (the in-app signing flow, used far more often) — now supersede any other `draft`/`sent` row of the same `kind` for that client in the same request. **Backfilled** the existing stale data with `scripts/backfill-supersede-stale-documents.mjs` (dry-run first, then `--apply`, transaction-wrapped with a post-update recheck): found and fixed exactly 2 rows across all 21 clients — Saffron's PAR-Q as expected, plus a second instance the diagnosis hadn't caught, Sam Gibbons' stale draft Personal Training Agreement. Verified live: Saffron's old PAR-Q now shows `Superseded`. `npx tsc --noEmit` clean throughout. Still belongs in the hub bug tracker once `project_id` is confirmed — not a registry blocker, just unmirrored. |
| CR-EF-027 | Session log should list in date order, be sortable, and be orderable by session number | CR (usability) | built | 2026-08-17 | Verified: `app/hub/(protected)/clients/[id]/page.tsx:125-131` pulled sessions across **all** of a client's blocks ordered by `session_number` descending, capped at 50 — so session 12 of block 1 interleaved with session 12 of block 3, no date ordering, no sort control. Fixed: server query now orders by `scheduled_at` (a real, sortable column — `completed_at` lives inside `data` JSONB and can't be ordered at the DB level); `TrainingTabContent.tsx`'s Session Log table now has clickable Date/Session headers with a client-side sort (Date = completed-or-scheduled, descending default; Session = block number then session number, so it doesn't reintroduce the interleaving bug). Also fixed in passing: `latestSessionLog` (drives the "Last Session"/"Last check-in" cards) used to blindly take `sessions[0]`, which under the new date-desc ordering could pick an upcoming uncompleted session; now derived by scanning for the session with the latest `completed_at`, independent of array order. Verified live against Emma Atkinson (#8, two blocks): Session sort correctly yields Block 1·S1→S3 then Block 2·S1→S2; Date sort correctly clusters dated sessions with an interleaved undated-but-scheduled row explained by the scheduled_at fallback. The mobile PWA (`app/hub/m/clients/[id]/page.tsx`) needed **no fix** — its "Recent sessions" card already sorts by `completed_at` in memory (`:227-234`); the missing `.order()` was only on the raw query, which never reached the user unsorted. It's a top-5 glance card by design, not a full sortable log, so no PWA change was made. |
| CR-EF-028 | Outlook duplicate-event problem — app-synced session events collide visually with Esther's own pre-existing personal Outlook entries for the same real appointment | CR (functionality, new module) | **built, verified on staging — awaiting go-ahead to promote to main** | 2026-08-17 | **Recovered 2026-08-17 — originally raised same day as CR-EF-016, overwritten by an ID collision with a concurrent session's own CR-EF-016–027 batch; renumbered, content unchanged.** Original root cause: the app implements a "schedule = live" design with no draft/tentative state — the moment a session's `scheduled_at` is set, `PATCH /api/sessions/[id]` immediately calls `syncSessionCalendarEvent()`, pushing a real event into Esther's connected Outlook calendar; the 15-min cron backstop will recreate a manually-deleted Outlook event if the DB still shows the session scheduled. **Re-raised live by Craig 2026-08-21** from a real screenshot of Esther's actual Outlook (Friday 21 Aug): "Emma Atkinson — Session 4" (app-synced) sitting directly next to "Emma" (Esther's own personal entry) for the same real appointment — visually a duplicate. Investigated same day: the app-synced event was created **2026-08-17**, the personal "Emma" entry has existed since **2022** — not a new bug, not caused by that day's CR-EF-050 work (verified via `session_calendar_events.synced_at` timestamps predating the session entirely). **Diagnostic run** (`scripts/diagnose-outlook-duplicate-events.mjs`, read-only, real connected calendar): of 12 app-synced sessions in the −1/+60 day window, **5 (42%) collide with a pre-existing Esther-typed personal entry** (Emma, and Monique across 4 separate session dates) — every personal entry's `createdDateTime` predates the app's own sync event, confirming the pattern is Esther's long-standing habit of manually noting recurring clients, not something the app is causing on its own. **Craig confirmed the fix approach 2026-08-21**: a human-confirmed reconciliation queue, same pattern as CR-EF-050's Bookings queue — before the push-sync creates a new event for a session, check for a same-day/name-matching pre-existing personal entry and surface it for Esther to either **link** (adopt the existing event, no duplicate created) or **confirm as genuinely separate** (both stand, push-sync proceeds normally). Rejected: silent auto-adopt (a wrong first-name match could silently overwrite a personal note) and confirm-before-sync-only (doesn't address today's actual case — Emma's was a real, deliberate booking, not a test session). **New user-facing hub surface — needs an Open Design mockup before build, no exemption** (same rule as CR-EF-048/CR-EF-050 precedent); brief written (`.context/brief-outlook-duplicate-reconciliation-opendesign.md`), **mockup delivered by Craig running Open Design himself 2026-08-21** (`hub-schedule-outlook-duplicates.html` — a mirrored tab bar alongside the Bookings queue, existing-event comparison with a same/off-time confidence flag). **Built same day**: `outlook_duplicate_candidates` migration (prod+staging); collision detection wired into both the batch cron and the immediate on-schedule push (`lib/calendar-sync.ts`) — pauses a session's sync instead of creating a duplicate; new `/hub/schedule/outlook/duplicates` queue matching the mockup 1:1, shared tab bar with the Bookings queue, combined route-in badge count. **Verification**: a read-only dry-run script confirmed the detection algorithm matches the 5 known real collisions and that shipping today changes zero currently-live sessions (everything already synced) — this only prevents *new* duplicates going forward, the 5 already-live ones are untouched. Link/keep-separate/undo all verified working end-to-end on staging with synthetic data (real UI clicks, DB-confirmed). **Also found and fixed, unrelated**: a pre-existing build-blocking bug in a concurrent mobile-calendar lane (`app/hub/m/calendar/page.tsx` passing a function prop across the Server→Client boundary) that was failing every deploy on this branch — fixed and verified with a full `next build`. Pushed to `staging`, deployed, verified live. Folded into `wo-ef-consolidated-2026-08-20` as Lane H. |
| CR-EF-029 | [BUG] Duplicate set-log writes — live logging paths (Train Screen, desktop logger, portal) send no `client_op_id`; retry-after-commit mints a fresh key; no per-set in-flight guard; portal INSERT has zero dedupe | Bug | built | 2026-08-17 | Confirmed in prod: **0 of 247 `set_logs` rows carry a `client_op_id`** — the 2026-08-13 idempotency layer has never deduped anything. Emma Atkinson triple-log 2026-08-17 confirmed in data (block 2 sessions 1+2). Evidence + fix plan (H1): `.context/assessment-workout-unification-2026-08-17.md` §1.1. Hub-tracker mirror pending project_id, as with CR-EF-002/026. **Re-verified 2026-08-18, still unfixed.** `TrainScreen.tsx`'s `saveSetLog` (:391-432): the direct online POST/PATCH `body` has no `client_op_id` field at all -- it is only added inside `enqueueOffline`, the offline-queue fallback. The primary, everyday write path -- a logged-in trainer or client tapping a set with a live connection -- still sends no idempotency key. H1 not built. **Built 2026-08-18** (`a2ca4d5`). Online set-log writes now carry client_op_id, portal route dedupes on it. Verified via hand-reviewed diff, not lane self-report. **Emma Atkinson's actual duplicate data cleaned up 2026-08-19**, Craig approved. Backup taken first (`.context/backups/emma-duplicate-logs-2026-08-19.json`, both sessions' full pre-cleanup state). Per the assessment's own reconstruction (Part 5, item 1): block 2 session 1's real morning workout (11:31-12:25, 20 rows) kept; the accidental 17:04-17:06 second pass (12 rows, mostly blank placeholders) deleted; session 1's 17:10 cancellation reversed (status -> completed, cancelled_at/cancel_reason cleared, completed_at reset to 12:25:25.647Z, `data.session_log.completed_at` synced to match). Session 2 (16:54-17:02, possible 14 Aug backfill) left untouched -- the reconstruction only flagged it as uncertain, it didn't propose a specific fix, and only Esther can say what it actually was. Verified via SQL (row counts, session status) and live in the hub UI (client #8, Block 2 Session 1 now shows "Completed 17 Aug," 4 of 13 exercises logged, first set matches the real morning data exactly). |
| CR-EF-030 | [BUG] Completed sessions revert to "in progress" — mobile TrainScreen PATCHes the whole `data` blob from a stale ref after completing; a note autosave or add-set wipes `completed_at` | Bug | built | 2026-08-17 | Root cause `TrainScreen.tsx:747-766` (no post-complete ref update; desktop's `onSessionLogChange` equivalent missing). Also: `started_at` stamped on screen mount, 4 surfaces derive "done" 4 different ways. Assessment §1.6, fix H2 + Phase 1. **Re-verified 2026-08-18, still unfixed.** `TrainScreen.tsx`'s `handleComplete` (:751-780) PATCHes `completed_at` to the server but never updates `dataRef.current`/`sessionLogRef.current` afterward -- the local refs still hold pre-completion state, so a later autosave can still overwrite the server's `completed_at` with the stale local value. H2 not built. **Built 2026-08-18** (`983cbcd`). handleComplete now syncs dataRef/sessionLogRef after the PATCH succeeds. |
| CR-EF-031 | [BUG] No guard on completed sessions — sets can be logged, prescription edited, and Complete re-pressed (overwriting RPE/fatigue/notes) on a finished session | Bug | raised | 2026-08-17 | No check in any API route or UI; only post-hoc audit (`set_log_revisions`). Assessment §1.5, fix H3 + Phase 1 (completed=read-only + audited Reopen). |
| CR-EF-032 | [BUG] Block week grouping meaningless — every manually-built block has all sessions in "week 1" (66/92 prod sessions); add/clone default or copy `week`; `week` not PATCHable; no Mon–Sun logic on any block surface | Bug | raised | 2026-08-17 | `blocks.scheduled_start` NULL on all 18 blocks. Proposal: derive weeks from `scheduled_at` (Mon–Sun), keep stored `week` as "Plan week" for unscheduled blocks — assessment §1.2, §3.2. |
| CR-EF-033 | [BUG] Session dates differ between client-detail tab and mobile — tab renders `completed_at` only (`—` when unlogged), mobile renders `scheduled_at`; NULLS-first DESC ordering can push logged sessions out of the 50-row window | Bug | built | 2026-08-17 | Assessment §1.3, fix H4. Timezone audited and ruled out. **Built 2026-08-18** (`fc8b4e5`). Desktop Sessions tab shows scheduled_at primary, completed_at secondary when it differs; NULLS LAST added to the sessions query. |
| CR-EF-034 | [BUG] Session naming inconsistent — `focus_label` ignored by client-detail tab (`Block n · Sn`) and session page; three independent archetype label maps; Outlook uses a fourth format | Bug | built | 2026-08-17 | Assessment §1.4, fix H6 + §3.5 (one name, one label map). **Re-verified 2026-08-18, still unfixed.** `SessionWorkoutLog.tsx` (desktop session screen) has no `focus_label` usage anywhere and no first-class `sessions.status` column read or written -- `started_at` is still stamped on mount, the exact pattern this CR and CR-EF-030 both describe as the root cause. H6 not built. **Built 2026-08-18** (`7a636cc`). Session screen h1 and browser title now use focus_label, session number kept as secondary context. |
| CR-EF-035 | [BUG] Schedule→client navigation broken — `ScheduleCalendar.tsx:244` and the retired `/hub/log` redirect build `/hub/clients/<UUID>/…` while client routes resolve by `client_number` (`parseInt(uuid)` → NaN) | Bug | built | 2026-08-17 | Assessment §1.8, fix H5. **Built 2026-08-18** (`d34c8ca`). Schedule links and the /hub/log redirect now resolve client_number instead of the UUID id. |
| CR-EF-036 | [BUG] Logged data invisible on client-page Sessions view — never reads `set_logs`; positional `exercise_ref` (`studio:`/`home:` prefix + array index) means surfaces can't see each other's logs and prescription edits orphan existing logs | Bug | raised — **backfill prerequisite done** | 2026-08-17 | `set_logs.exercise_uid` column exists. **2026-08-19: backfilled to 100% coverage** (`scripts/backfill-exercise-uid.mjs`, all 156 remaining rows resolved cleanly against their session's own data JSONB, 0 unresolvable). The uid-keyed identity prerequisite is now in place; this CR's actual fix — the client-page Sessions view reading `set_logs` at all — still needs building. |
| CR-EF-037 | Unified session state model + calendar-spine redesign — first-class `status`/`started_at`/`completed_at` on `sessions`, single transition API, completed=read-only+Reopen, uid-keyed set_logs, one write path with mandatory `client_op_id`, date-derived Mon–Sun weeks, block-page scheduling, completion on the calendar, Trainerize-informed PWA navigation | CR (architecture + design) | **approved — G2 signed 2026-08-19** | 2026-08-17 | **G2 signed by Craig 2026-08-19**, along with adopting the Part 3 unified assessment/workout data model (`qmsxl5j0c7l` cleared, all hotfixes AUTO). Phases 2–3 are now buildable lanes. The umbrella for CR-EF-029–036's structural fixes. Full proposal: `.context/assessment-workout-unification-2026-08-17.md` Part 3–4. Rides `wo-ef-workout-consolidation-pwa-2026-08-15`. 2026-08-17 evening: Open Design delivered revisions to `hub-block-module.html`/`hub-schedule.html`/`hub-session.html` + new `hub-client-sessions-tab.html` against `brief-workout-unification-opendesign.md`. Design Parity Gate review: **core state model solid** (shared 5-state pill byte-identical across all four, completed/cancelled read-only correct, in-progress triggers on first set logged as specified) — **4 gaps found, not self-flagged by the handback**: session screen names itself "Session 10" not `focus_label` (breaks the brief's own naming rule, on the one screen the trainer actually uses); day/month schedule views disagree on cancelled-session visibility and don't cross-link; 2 of 5 named "honest data cases" (off-pattern week pileup, 18-session block) missing from sample data. Revision request sent: `.context/revision-request-workout-unification-2026-08-17.md` (mirrored + committed `design-systems@6512f1b`). Do not sign G2 until the revision lands. **Revision verified landed, 2026-08-18.** Re-checked the 3 content gaps from the 17 Aug G2 gate review directly against the committed mockups: (1) `hub-session.html` now renders `focus_label` ("Workout A") throughout, zero "Session 10"; (2) `hub-schedule.html`/`hub-schedule-month.html` now explicitly agree on cancelled-hidden-by-default and cross-link; (3) `hub-block-module.html` now carries the 18-session schema-cap block with a 5-session pile-up week. All 3 confirmed via direct file read, not self-report. Gap 4 (self-flagged deviations in the handback) not independently verifiable from file content alone. **Design side of G2 looks ready to sign** -- the app-code build (state-model migration, transition API, uid-keyed logs, one write path) has not started; that is the actual remaining work once Craig signs off.

**G2 SIGNED by Craig, 2026-08-19.** Phase 2 started same day: (1) `lib/session-transitions.ts` -- `markSessionInProgress()`, an idempotent guarded UPDATE called after every set_logs insert (staff + portal), flipping `status`/`started_at` the moment a session's first set is logged. Fixes a live drift risk found in the process: the Phase 1 migration shipped without its own "transition API," so nothing had written the new columns since 18 Aug -- verified 0 sessions had drifted yet, but every completion from today onward would have. (2) `app/api/sessions/[id]/route.ts`'s existing PATCH handler (the one both `TrainScreen.tsx` and `SessionWorkoutLog.tsx`'s `handleComplete` already call) now also syncs `completed_at`/`status` when `data.session_log.completed_at` is set, and `status='cancelled'` when `cancelled_at` is set -- no frontend changes needed. Verified via a synthetic throwaway session (created, transitioned twice to prove the idempotency guard, deleted -- zero real client data touched) and direct code-path tracing (mount-time `started_at` writes confirmed NOT to false-trigger the completion sync). (3) SessionStatusPill shared primitive built (Lane B, see CR-EF-039) -- Phase 2's UI now has the component it needs. Merged to `staging`, not yet promoted to main/live. **Still to come:** wiring SessionStatusPill into live screens, CR-EF-036's actual fix (client Sessions view reading set_logs), H3's write-guard + Reopen UI (needs its own Open Design read of the `reopen-dialog` mockup section before building), Phase 3 (calendar spine).

**PROMOTED TO MAIN 2026-08-19 (`2c6c63d`).** Craig approved the standing promotion decision. The full redesign (all 7 lanes of `wo-ef-sessions-blocks-full-build-2026-08-19`: SessionStatusPill wired into session/block/schedule/client-tab screens, H3 write-guard + audited Reopen, CR-EF-036 Sessions logged-data visibility, Phase 3 calendar spine with derived Mon–Sun weeks + block-page schedule panel, HubRail + 8-screen consistency sweep) was merged from staging to main and pushed (`ee1c21b..2c6c63d`, 44 files, +1597/−516). Auto-deploy triggered. Only merge conflict was the append-only `loop-status.md` ledger (resolved keeping both sides). This resolves the live-hub-vs-mockup deviance that was the bulk of the user's report. Remaining narrow gaps (training-blocks list columns/status-map, workout-templates drawer/assign/stepper/difficulty=position, derived Est. duration, minor styling remnants) tracked by `.context/workorder-hub-workout-parity-2026-08-19.md`. |
| CR-EF-038 | [BUG] Agreements pages deep-link to `/hub/clients/[id]?tab=profile-compliance`, a tab not in `VALID_TABS` — all three links silently land on Overview | Bug | raised | 2026-08-17 | `agreements-table.tsx:92`, `AgreementDetailClient.tsx:294,589` vs `ClientDetailTabs.tsx:43`. Small fix: point at the real tab (`admin`) or add the alias. Found by the 2026-08-17 hub structure audit. |
| CR-EF-039 | Hub UI pattern consolidation — one accordion, one card contract (flex h-full + flex-1 body, no items-start bands), one tab component, tables→HubTable, headers→HubPageHeader, pills→StatusBadge, shared 5-state session status pill (desktop+mobile), retire dead/local copies (`HubSection` unused, `CollapsibleSection` local fork, 3 retyped tab strips, 2 KpiTile rebuilds) | CR (design-consistency, umbrella) | **approach approved 2026-08-19 — Lane B + full sweep** | 2026-08-17 | **Craig approved 2026-08-19: build Lane B shared primitives, then commission the full ~50-screen consistency sweep** (only 5–6 of ~50 hub screens had ever been briefed; sweep will run as grinding lanes past today). Census: 8 accordion patterns, 5 card styles, 6 tab styles, 28 raw tables, 4 title sizes, 3 chevron conventions; desktop/mobile share zero components. Full audit: `.context/audit-hub-structure-consistency-2026-08-17.md`. WO `wo-ef-hub-structure-consistency-2026-08-17` Lanes B+C. **Partially built 2026-08-18** -- dashboard, client record, block module, schedule (day+month), and mobile Today all got the qa-bar/accordion treatment, but the client-record and block-module code (`c44fa0c`, 13:51) was committed 1h43m before the mockups revising them landed (15:34-15:37) and was never re-checked against them once they existed. Verified deviations from the design-parity review: CR-EF-042 (dashboard missing a button), CR-EF-043 (client-record bar/header split not followed), CR-EF-044 (block-module button set differs), CR-EF-046 (mobile Today class-contract mismatch, cosmetically invisible). Session screen (`hub-session.html`) has zero qa-bar/hub-section adoption on either side -- correctly still outstanding, not a build failure. **2026-08-19: Craig approved the full ~50-screen sweep. Lane B started** -- `components/hub/SessionStatusPill.tsx` built (dispatched to OpenCode, hand-reviewed, independently render-verified against all 5 mockup states, not lane self-report), byte-matched to `hub-session.html`'s pill spec. Not yet wired into any live screen. HubRail, HubCard height contract, single accordion, and single tab component are still open Lane B units. |
| CR-EF-040 | Right rail as first-class shell concept — shared `HubRail` primitive (one width/breakpoint/sticky), Quick Actions promoted to the rail's top slot on major surfaces with one visual variant; phase 2: rail-as-navigation IA concept via Open Design after the consistency sweep | CR (design + IA) | partially built, deviations found | 2026-08-17 | Today: 4 rail widths, 4 breakpoints, sticky on 1 page of 5, no shared component; the hub's only 2 Quick Actions panels use opposite variants. Audit §3. **Partially built 2026-08-18** -- see CR-EF-039's note; the two CRs shipped together in the same three commits and share the same evidence. |
| CR-EF-041 | Mockup register version-control + revive drift detection — Open Design pass per `.context/request-opendesign-mockup-version-control-2026-08-17.md` (archive 5 superseded + 3 speculative, relocate 7 strays, refresh 4 drifted, regenerate index.html as the living register with standing rules) + repair `.context/tools/verify-hub-pages.js` (non-recursive readdir finds zero mockups since the subfolder reorg and hard-crashes on ENOENT; ~20 join-table rows missing incl. all mobile; 6 false "intentionally unmocked" claims; stale /hub/log and parq rows) | CR (process + tooling) | **Open Design pass built + verified** | 2026-08-17 | Lane A (Open Design pass) delivered same day and verified compliant against the request: all 8 files archived exactly as specified with a documented `_archive/README.md`, all 7 strays relocated, `index.html` regenerated as a living register (status badges + standing rules), plus it caught and correctly fixed a dead-sidebar-link consequence of the archival across every affected mockup — verified via git diff, not taken on report. Committed `design-systems@24291ea`. Lane D (`verify-hub-pages.js` repair) still outstanding — not part of Open Design's scope, needs a code lane. |
| CR-EF-042 | [BUG] Dashboard quick-actions bar is missing "Log check-in" -- mockup specifies 4 buttons, live has 3 | Bug | **built, verified on development** | 2026-08-18 | Found by design-parity review. Mockup (`hub-dashboard.html`, `data-od-id="quick-actions"`): New client (primary), **Log check-in**, Browse exercise library, View all clients. Live (`app/hub/(protected)/page.tsx:248`): New client, Browse exercise library, View all clients -- "Log check-in" was never added to the `HubQuickActions` actions array. Same 3-button set (missing Log check-in) also confirmed on `/hub/schedule` -- but the schedule mockups never included it in the first place, so that page is not a bug, just consistent with its own spec. **Coded 2026-08-18** (`efe099d`), stranded on unmerged branch `claude/lane-cr-042-046-parity` despite the register having claimed "merged to main". **Corrected and merged to `staging` 2026-08-19** (`a50029d`); verified live via claude-in-chrome on development.eternal-fitness.co.uk -- Quick Actions bar now shows 4 buttons matching the mockup exactly. Not yet promoted to main/live. |
| CR-EF-043 | Client-record quick-actions bar violates the mockup's explicit header/bar split -- Edit Client and New Session should sit in the page header, not the qa-bar | CR (design-consistency) | built | 2026-08-18 | Found by design-parity review, on the screen built 13:51 -- 1h43m before its mockup landed at 15:37. Mockup (`hub-client-detail-refined.html`) carries an explicit authoring comment: "Quick actions bar sits above the client identity header, not inside it... Edit Client / New Session stay in the header itself (contextual primary actions); these are the cross-navigation shortcuts previously buried in the right rail" -- 3 buttons in the bar (Plan Block, Send Update, View Documents), 2 in the header. Live has all 5 in the bar (`app/hub/(protected)/clients/[id]/page.tsx`), nothing in the header. Direct evidence the client-record build did not reconcile against its own mockup once it existed. **Built 2026-08-18** (`cb1c7f5`, merged to main). Edit Client and New Session moved into the client identity header; bar reduced to Plan Block/Send Update/View Documents. |
| CR-EF-044 | Block-module quick-actions button set/order/primary differs from its mockup | CR (design-consistency) | built | 2026-08-18 | Found by design-parity review, same sequencing gap as CR-EF-043 (code `c44fa0c` 13:51, mockup 15:34). Mockup (`hub-block-module.html`): **Schedule block** (primary, first) + Edit Block, plus an overflow "..." menu (Print/Export/Delete). Live (`BlockActions.tsx`): Edit block (first) + Add workout + Schedule -- "Add workout" is not in the mockup's visible bar at all, and which button is primary/first differs. Needs a design decision (does Add workout belong in the bar, the overflow menu, or neither) before code changes, not a straight match-the-mockup fix. **Built 2026-08-18** (`5e75ee7`, merged to main). Bar reordered to Schedule (primary)/Edit Block/Add workout, matching the mockup's primary action with Add workout kept non-destructively. |
| CR-EF-045 | Client-record tab set and badge placement diverge from the mockup -- needs a decision, not a code fix | CR (design-consistency) | built | 2026-08-18 | Found by design-parity review. Mockup (`hub-client-detail-refined.html`) has 5 tabs (Overview/Profile/Admin/Training/Comms) with an unread-count badge on **Comms**. Live (`ClientDetailTabs.tsx`) has 6 tabs -- adds **Plan Agent**, not in the mockup at all -- and its badge sits on **Admin** (outstanding-actions count), not Comms. Per the parity spec's own rule ("a behaviour the app has and the template doesn't is a feature to keep and report, not drift") the extra tab is not a defect -- the mockup needs reconciling to add it. The badge relocation is a genuine open question: is the Admin-actions badge right and the mockup's Comms-unread badge stale, or does the app need both? **Built 2026-08-18** (`design-systems@5e476da`). Mockup fix, not app code -- Plan Agent tab added (CONCEPT placeholder), badge moved Comms→Admin to match the live app. |
| CR-EF-046 | Mobile Today's accordion reuses TrainScreen's `.sec`/`.sec-h`/`.sec-b` classes instead of the mockup's own `.m-section`/`.m-section-h`/`.m-section-b` contract | CR (design-consistency, cleanup) | **built, verified on development** | 2026-08-18 | Found by design-parity review. Structurally and visually the two sections (Sessions, Tasks due) match the mockup exactly -- same order, same open/closed defaults, same labels -- confirmed live via `https://development.eternal-fitness.co.uk/hub/m`. The divergence is implementation only: `app/hub/m/TodayScreen.tsx` was built against a class family the mockup does not define, rather than the `.m-section` contract `mobile/today/hub-m-today.html` actually specifies. Low risk (no visible bug) but exactly the kind of "local fork" CR-EF-039 was raised to retire -- fold into that CR's scope rather than treating as separate work. **Coded 2026-08-18** (`6edf7f2`), stranded on unmerged branch `claude/lane-cr-042-046-parity` despite the register having claimed "merged to main". **Corrected and merged to `staging` 2026-08-19** (`a50029d`); verified via `document.querySelectorAll` in claude-in-chrome on development.eternal-fitness.co.uk that `TodayScreen.tsx` now renders `.m-section*` classes, not `.sec*`. `.m-section*` selectors aliased onto the shared `.sec*` rule set in mobile.css; TrainScreen untouched. Not yet promoted to main/live. |
| CR-EF-048 | Endurance/multi-discipline training blocks (calendar-based, hours-only, manually editable — no AI generation) for clients like Rick training toward a race, replicable per client | CR (functionality, new module) | **built, verified live on production** | 2026-08-19 | Raised from Esther's Otter call ("Training Plan Template Requirements", 2026/08/19 08:43, https://otter.ai/u/8TrEG4KrYlCuB96lJsjT2KAfyNU) plus the reference doc she had Craig build same-day, `Rick_Block1_Plan_17Aug-5Sept2026.docx` (OneDrive `decoded-ops-ai\eternal-fitness\clients\`). Esther's explicit requirements (her words, transcript): "everything needs to be editable... by me, not by you, and not by an AI agent... because AI agents are too slow, and time is money" — she wants a manual-entry/self-service template she can renew herself every 2-3 weeks without asking Craig, no AI generation step, no logging fields (client shares Garmin data with her directly). **This is a distinct model from the existing strength Plan Agent** (`blocks`/`sessions`, `supabase/migrations/20260509_training_app.sql`): that schema is rigidly fixed at 6 weeks x 3 sessions with sets/reps/tempo (`Exercise` type, `types/index.ts:213-241`) and has no concept of dates, durations, or non-strength disciplines (run/bike/swim). The reference doc's actual shape: a free-text "Direction" section (weekly hour targets per discipline + coaching notes, e.g. "Swim: ~1-1.5 hrs/week, open water"), then a day-by-day calendar table (Date / Day / Run / Bike-hours / Swim-hours / Notes) with weekly-summary rows interspersed, block length variable (17 days here, not fixed to 6 weeks). Proposed fit: a new `client_documents` kind (document engine already supports arbitrary structured `body` JSONB per kind, `lib/documents/types.ts:41-48`, `supabase/migrations/20260704_document_engine.sql`) rather than extending `blocks`/`sessions` — this is closer in shape to a document Esther authors and re-issues than a session-logged training block, and reuses the existing send/resend/version/supersede machinery for free. Needs: (1) Esther confirms this framing before build, (2) an Open Design mockup for the editor (new user-facing surface, no "it's just a form" exemption per project rules), (3) confirm whether the calendar table should be a structured editable grid or a rich-text section like other document kinds' `sections[].html`. **Open Design mockup delivered 2026-08-19** — project `ef-endurance-block-editor-cr-ef-048-fad0`, run `e6a37cc1-cc76-4549-9cc2-4022d34e8d81`, single self-contained file `endurance-block-editor.html` (`D:\apps\design-systems\ef-endurance-block-editor-cr-ef-048-fad0\`) with a flow switcher across all 3 states (Document Register entry point, Esther's edit view, client-facing read view). Built against the real EF token set (`--color-ink/cream/warm/rose/teal/amber`). Sent to Craig for review. **Built 2026-08-19** (OpenCode lane `cr-ef-048-endurance-block`, hand-reviewed line-by-line before merge, not trusted on self-report): new `endurance_block` document kind — `EnduranceBlockEditor.tsx` (spreadsheet-style Direction panel + editable calendar grid, week-summary/brick/race row treatments), read-only rendering shared by the client view and public sign page, a Renew endpoint that clones a block forward with dates advanced by the source block's own day-span, and a guarded idempotent `document_templates` seed migration. Migration run against prod (`scripts/run-endurance-block-migration.mjs`, before/after row-count asserted, 10→11). Live-verified end-to-end before push: created a real block on Rick Frenken (client #23) via the hub UI, filled Direction/targets/coaching-note/dates, saved, confirmed exact values persisted via direct DB query, confirmed the public sign page renders correctly (target-event/dates strip, Direction bullets, amber coaching-note callout, calendar headers, correct no-signature messaging) — matches the mockup's chrome and EF token set. Test document deleted after verification, Rick's file left clean. Pushed to `staging` (`7ee105f`, then docs-only `f35780b`) — deploy finished 18:21:57, **confirmed live on development.eternal-fitness.co.uk**: "Endurance Training Block" selectable in the New Document dropdown on a real staging client. Note: staging runs its own DB clone (`eternal_fitness_staging`, same Postgres host as prod, different database name/user) — the prod migration does NOT apply there automatically; had to run `scripts/run-endurance-block-migration.mjs` a second time by hand against `postgresql://ef_staging_app:...@localhost:5433/eternal_fitness_staging` (reachable through the existing prod SSH tunnel, just a different db name). Also: staging's client roster is a separate, older snapshot — client #23 (Rick) doesn't exist there, tested against client #17 instead. Worth remembering for every future CR that touches schema.

**Final fix, same day (Craig's review of the real Rick block):** the hub editor's Signatures card was unconditional, showing "Client: Awaiting client signature" even though `requires_client_signature`/`requires_trainer_signature` are both `false` for this kind — implied a signature was pending when none was ever required. Gated the card on those two flags (`DocumentDetailClient.tsx`). Verified scoping both ways: Rick's real endurance block no longer shows it; his signed PAR-Q still correctly shows "Signatures / Client: Signed by Rick Frenken on 2026-08-17". Pushed `0817d29` to staging then main, **deploy confirmed finished** (Coolify deployment `zglyc0glv1gyv8uxn6yejphd`, finished 19:35:20) and **verified live on eternal-fitness.co.uk** — Rick's real block (`6cec52e1`) now goes straight from the calendar to "Send to client," no Signatures section.

**Fourth round of fixes, same day (Craig: still bunched up + print/PDF clipping columns):** (a) **column misalignment — the serious one.** `.eb-sum-label` carried `display: block` on a `<td colspan="2">`; a block-level table cell drops out of the row's cell sequence, collapsing the colspan and shifting **every week-summary value one column left** — the week's run hours printed under "Day", bike under "Run", swim under "Bike". Read-view/client-facing only (the editor's summary cell carries no class, so it always rendered correctly, which is why it went unnoticed). Verified by measuring each cell's left edge against its header column, before and after. (b) `.doc-sheet--wide` 1180px → `min(1560px, 100vw - 48px)`, plus explicit calendar column proportions (Date 76px / Day 56px / Run-Bike-Swim 16% each / Notes remainder) and table min-width 720→940px, so a busy Run cell no longer wraps five lines beside an empty half-width Swim cell. (c) **print/PDF was clipping columns**: the calendar is a `min-width: 720px` block inside an `overflow-x: auto` wrapper — on paper there is nothing to scroll, so the Notes column was simply cut off, and portrait A4 only offers ~180mm anyway. Added a named `@page eb-wide { size: A4 landscape }` scoped via `.doc-sheet--wide { page: eb-wide }` (other kinds keep the portrait default), and a print block that releases min-width/overflow, switches to `table-layout: fixed` with per-column percentages, repeats the header per page, avoids mid-row breaks, and hides editor-only chrome. Verified at simulated A4-landscape printable width (1047px): table lays out at 965px, `clipped: false`, all six columns present and aligned. (d) removed a hardcoded "Week total: " prefix in the read view that double-printed against Esther's own cell text ("Week total: Week total: ~2.5-3.25 hrs").

CR-EF-048 is complete: built, mockup-matched, live in production, real data on Rick's profile, and four rounds of live-feedback fixes (truncation, sheet width, signatures, then column alignment + print) all shipped same day.

**Two follow-up fixes, same day (Craig's live feedback on the populated Rick block):** (1) calendar/target cells were single-line `<input>`s that clipped long content invisibly instead of wrapping — replaced with an auto-resizing `AutoCell` textarea, verified live editor + read view. (2) the shared `doc-sheet` prose width (780px) squashed the 6-column calendar to one word per line — added an opt-in `doc-sheet--wide` (1180px) applied only for `kind === "endurance_block"`, every other document kind untouched. Both pushed to staging (`bd7699f`, `391e796`) and verified live before promotion.

**Promoted to `main` 2026-08-19 19:12** (fast-forward `391e796`, staging was a clean ancestor of main — 4 commits promoted, 2 of them unrelated CR-EF-071 work from a concurrent session already on staging). Prod deploy triggered automatically; prod DB already carried the `endurance_block` template (migration was run there first, before the staging-DB-is-separate discovery). **Deploy confirmed finished** (Coolify: deployment `ox0y9gceglbunwmv4tlxohzc`, commit `391e796`, finished 19:18:33) and **verified live on eternal-fitness.co.uk**: "Endurance Training Block" present in the New Document dropdown on Rick Frenken's real documents page.

**Real deliverable created 2026-08-19** — the actual "Rick — Block 1: Arrival to First Event" document (full content from `Rick_Block1_Plan_17Aug-5Sept2026.docx`: target event, 17 Aug–5 Sept date range, Direction paragraph + 5 weekly targets, two-brick coaching note, all 21 calendar rows including both brick sessions and race day) now lives on Rick's actual production client profile (`scripts/create-rick-block-prod.mjs`, doc id `6cec52e1-1432-4441-b2d2-8fa6f958cf07`), not just a deleted test copy. Confirmed rendering correctly live in the hub editor — still a draft, nothing sent to Rick yet.
| CR-EF-047 | Docker build has no Next build-cache mount -- every deploy recompiles every route from scratch | CR (build performance) | **rejected -- measured, does not work** | 2026-08-19 | Raised from the Coolify deploy-pipeline review (`wo-deploy-pipeline-speedup-2026-08-19`) on the theory that this app was the slowest Next build on the estate (191s vs decoded-data-app 128s and decoded-ops-hub 30s) **because** it was the only one without a `type=cache,target=/app/.next/cache` mount. Built and shipped (`4c45976`), then measured and **reverted the same day** (see revert commit). The theory was wrong. Build-step timings pulled from BuildKit deploy logs: **before** the change, 10 deploys spanning 08-18 to 08-19 ranged 179.9-227.6s, mean ~205s; **after**, 3 deploys came in at 234.2s, 235.9s and 214.8s, mean ~228s -- at or above the top of the pre-existing range. The 214.8s figure is a clean solo force-rebuild on an otherwise-idle builder with a warm cache mount, so it is not contention noise. Cause: every deploy does a full `COPY . .`, so this build is dominated by fresh compilation rather than cacheable artifacts, and the extra `RUN` layer costs more than the mount returns. The 30s/128s/191s spread across the three apps is **app size, not caching**. A `pnpm` store-dir mount was considered and never shipped (pnpm hardlinks node_modules out of the store; a cache mount is a separate filesystem, so it relies on an unproven copy-fallback). Kept as a rejected CR rather than deleted so nobody re-tries it: a warning comment now sits in the Dockerfile at the exact line. **Real lever for this app is CPU, not caching** -- see the deferred build-server item on the WO. |
| CR-EF-069 | Dashboard "Do Not Train" / "Action needed" / "Gone quiet" client-alert lists render flat/expanded; Open Design mockup has them in an accordion | CR (design-consistency) | **built, pushed to staging** | 2026-08-19 | Raised by Craig live against development.eternal-fitness.co.uk. Built via OpenCode lane (`lane-dashboard-parity`, hand-reviewed diff), pushed to `staging` (`298899c`). Verified against `hub-dashboard.html` directly: mockup's `alerts-accordion` wraps Do Not Train/Action needed/updates-due in one `HubAccordionSection` (reused existing shared component, no fork) — implementation matches exactly, including the "N alerts" + breakdown subtitle format. Note: mockup has no "Gone quiet" alert (app-specific, not in the mockup) — all 4 live banners folded into the one accordion rather than leaving a stray flat banner. `tsc --noEmit` clean. **Verified live on development 2026-08-19** (accordion + Recent blocks confirmed via claude-in-chrome, client #8). Not yet promoted to main. |
| CR-EF-070 | Dashboard "Recent blocks" list lacks context — shows only "Block 1"/"Block 2" + "Draft" status, no client name or block title, so it's unclear who each block is for | CR (usability) | **built, pushed to staging** | 2026-08-19 | Raised by Craig live against development.eternal-fitness.co.uk. Built via OpenCode lane (`lane-dashboard-parity`, hand-reviewed diff), pushed to `staging` (`298899c`). Each row now shows "{client name} — Block {N}" plus `block_note` as a subtitle, using the existing `blocks.block_note` schema field (no new column needed). `tsc --noEmit` clean. **Verified live on development 2026-08-19**. Not yet promoted to main. |
| CR-EF-050 | Outlook↔`/hub/schedule` gap — clients book sessions via a **Microsoft Bookings form**, landing directly in the same Outlook calendar the app syncs to; those bookings need reconciling into the hub's schedule/clients, not just diagnosed | CR (functionality, new module) | **built, verified live on production** | 2026-08-19 | Raised by Craig live against development.eternal-fitness.co.uk; scope clarified 2026-08-20. Confirmed structural: sync is one-way app→Outlook only (`lib/calendar-sync.ts:14-19`, `lib/graph-client.ts` has no read/list-events call), and `/hub/schedule` renders purely from `sessions` — so a Bookings appointment has no `sessions` row and is invisible to the app by design. **Craig's answers, 2026-08-20:** (1) Bookings appointments land in the **same Outlook calendar** already selected under Settings → Integrations — no separate Bookings calendar/mailbox to poll. (2) Match to an existing client **by email** against `clients.email`; auto-link on a match, flag for manual review in the hub when no match is found. (3) Scope as a CR + Work Order, not a quick fix — new Graph read-back capability, a matching/reconciliation UI, and (per this project's CR-EF-048 precedent) a new user-facing hub surface needs an Open Design mockup before build, no "it's just a list" exemption. Folded into `wo-ef-consolidated-2026-08-20` as Lane F — see that WO for units/gates. Related: **INT-002** below (native Bookings picker) is a different, smaller integration (replacing the client-facing booking widget) — this CR is about reading back what Bookings already wrote to Outlook, not the picker itself. **Diagnostic run 2026-08-20** (`scripts/diagnose-outlook-bookings-gap.mjs`, read-only, against the live connected calendar, −1/+60 day window matching `calendar-sync.ts`): 221 Outlook events in window, 217 have no `session_calendar_events` mapping. Of those 217, only **17 are genuine Microsoft Bookings appointments** (organizer `EternalFitnessBookings@eternal-fitness.co.uk`, subject `"Personal Training - {name}"` / `"Initial consult - {name}"`, 7 distinct clients: Becky Price, Camilla Arnold, Ellie Wallwork, Nathan Wadey, Rick Frenken, Tom Putnam ×2 subjects). The other **200 are Esther's own long-standing personal calendar entries** (organizer `esther.fair@eternal-fitness.co.uk`, single first names like "Ian"/"Emma"/"Sarah"/"Anne"/"Monique", plus non-client entries like "0FF"/"BLOCK OUT"/"LONDON"/blank subjects, some dating back to 2021) — pre-existing Outlook usage unrelated to the app or to Bookings, not part of this CR's actual gap. **Email-matching decision (2026-08-19) is disproven by the real data and needs revisiting**: every genuine Bookings event's organizer/attendees are internal addresses only (`EternalFitnessBookings@...` and `esther.fair@...`) — the client's real email never appears on the event at all, confirmed against `clients.email` for all 7 names (0/17 would have matched by email). What **does** work: all 7 client names parsed from the event subject matched an existing `clients.name` row exactly (verified by direct query). Revised proposal: match by **parsing the client name out of the Bookings subject line** (`"Personal Training - {name}"` pattern) against `clients.name`, not by email; still flag anything ambiguous (no subject match, or more than one client with a similar name) for manual review in the hub, same as originally intended. Craig confirmed the revised matching approach 2026-08-20. **Built and deployed same session**: `outlook_booking_events` migration (prod + staging), Graph read-back, name-matching, `/hub/schedule/outlook` reconciliation queue matching the mockup's 3 row states, route-in badge on both calendar views. Live-verified end-to-end against real production data (all 7 real clients auto-matched correctly; one real appointment confirmed into a real, correctly-scheduled session that adopted the existing Outlook event rather than duplicating it). **Found mid-session: staging's Microsoft integration was connected to Esther's real Outlook account/calendar since 2026-08-15** (unrelated pre-existing state, the root cause of the still-open CR-EF-028) — disconnected it before any further staging testing, so staging can no longer reach real Outlook in either direction. Promoted to `main` and confirmed live on eternal-fitness.co.uk (Coolify deployment on the exact pushed commit, page loaded directly showing the real remaining unmatched bookings). |
| CR-EF-051 | No way to assign a workout/template to an existing session that has a block but no exercise content yet (e.g. historical bookings) | CR (functionality) | raised | 2026-08-19 | Raised by Craig live during the same schedule-page review as CR-EF-050. Confirmed: every session requires a `block_id` (NOT NULL, `20260509_training_app.sql:74-82`) so a session always exists, but `data` JSONB can be empty/placeholder (no exercises assigned). The only existing mechanism, `AddWorkoutDialog.tsx` (`app/hub/.../blocks/[blockId]/AddWorkoutDialog.tsx:59-66`), creates a **new** session from a template — it does not retrofit an existing empty one. Scope not yet sized: needs a "retrofit assign" action, and possibly a one-off pass to find/flag historical sessions currently sitting empty. Likely rides the same session/workout surfaces as CR-EF-011's consolidation — not briefed or built. |
| CR-EF-052 | Client detail page — Admin tab (`/hub/clients/8?tab=admin`) doesn't match `hub-client-detail-refined.html` | CR (design parity) | **built, pushed to staging** | 2026-08-19 | Raised by Craig live. Built via OpenCode lane (`lane-client-tabs-parity`, hand-reviewed diff), pushed to `staging` (`e2f9513`). Fix: split the single Compliance Status card (which had Documents nested inside it) into 3 sibling cards — Compliance Status (now a flat summary grid: overall status, PAR-Q/GP-clearance/agreement completion dates, outstanding count, next review — all resolved from existing data, none fabricated), Documents, Package & Payments — matching the mockup's card layout. `tsc --noEmit` clean. **Verified live on development 2026-08-19**. Not yet promoted to main. |
| CR-EF-053 | Client detail page — Training tab, Blocks view (`/hub/clients/8?tab=training&view=blocks`) doesn't match mockup | CR (design parity) | **built, pushed to staging** | 2026-08-19 | Raised by Craig live. Fix: "Sessions" column (bare count) renamed "Progress" ("N sessions") per mockup. Block name kept as "Block N" — `blocks` has no name column, only `block_number`/`block_note`/`summary`, so no fabricated data was added. Pushed `staging` (`e2f9513`), `tsc` clean. |
| CR-EF-054 | Client detail page — Training tab, Sessions view doesn't match mockup | CR (design parity) | **closed — not a defect** | 2026-08-19 | Investigated via OpenCode lane: the live Session Log table (Date/Session/Status/Logged/Check-in/Note + expandable set evidence, per CR-EF-027/036/037) is a functional superset of the mockup's simpler Date/Block/Duration/Format table. Per the parity rule already established by CR-EF-045 ("a behaviour the app has and the template doesn't is a feature to keep and report, not drift"), left unchanged — regressing to the mockup's 4 columns would delete working functionality. |
| CR-EF-055 | Client detail page — Training tab, Progress view doesn't match mockup | CR (design parity) | **closed — not a defect** | 2026-08-19 | Investigated via OpenCode lane: live `ExerciseTrendsPanel` + `ExerciseHistoryPanel` (with a real Personal Bests table + Last Performed) is the real-data equivalent of the mockup's chart-placeholder + PB-table concept. Functionally superior, left unchanged, same rule as CR-EF-054. |
| CR-EF-056 | Client detail page — Training tab, Pre-app history view doesn't match mockup | CR (design parity) | **closed — not a defect** | 2026-08-19 | Investigated via OpenCode lane: live `TrainerizeHistoryPanel` is the working version of the mockup's "Pre-App Training History" placeholder card (Trainerize genuinely is the pre-app source); labels already match. Left unchanged. Note: the view's URL param is `history`, not `pre-app-history` as originally assumed when this CR was raised — `ClientDetailTabs.tsx` `VALID_VIEWS.training = ["blocks","sessions","progress","history"]`. |
| CR-EF-057 | Client detail page — Comms tab, Updates view (`/hub/clients/8?tab=comms&view=updates`) doesn't match mockup | CR (design parity) | **built, pushed to staging** | 2026-08-19 | Raised by Craig live. Fix: dense "6-Week Updates" table replaced with a per-update card list (subject, timestamp, status pill, body excerpt, row actions) matching the mockup's `.update-item` shape; title "6-Week Updates"→"Client Updates", button "New Update"→"Send Update". **Claude's own manual fix on top of the OpenCode diff**: the lane's version dropped the "Emailed" indicator entirely — restored as a pill next to status for sent updates, since `emailed` is this project's definitive signal of whether an email actually sent (a standing, unconfirmed-reliability concern per this repo's CLAUDE.md), too operationally significant to lose in a parity pass. Pushed `staging` (`e2f9513`), `tsc` clean. **Verified live on development 2026-08-19**: card list + "Emailed" pill confirmed. Caveat found live: the body excerpt surfaces raw email-template boilerplate ("EF Eternal Fitness Worthing · West Sussex..." bleeding in ahead of the real content) — pre-existing in `body_html`, just newly visible since the old table never showed a body excerpt at all. Not a regression this CR introduced; worth its own CR if it reads badly to Esther. Not yet promoted to main. |
| CR-EF-058 | Client detail page — Comms tab, Tasks view (`/hub/clients/8?tab=comms&view=tasks`) doesn't match mockup | CR (design parity) | **built, pushed to staging** | 2026-08-19 | Raised by Craig live. Fix: dropped the nested 12-col grid's internal "Summary" count rail (To do/In progress/Done counts), single "Tasks" card per mockup — add/toggle/delete + derived check-in reminder all kept. Pushed `staging` (`e2f9513`), `tsc` clean. **Verified live on development 2026-08-19**. Not yet promoted to main. |
| CR-EF-059 | Block module page (`/hub/clients/8/blocks/[blockId]`) not using the design or logic of `hub-block-module.html` | CR (design parity) | **built, pushed to staging** | 2026-08-19 | Raised by Craig live against development.eternal-fitness.co.uk. **Claude's own live spot-check first (not element-scoped — see WO ledger)**: the page's major sections were already present; the real gaps were a redundant "(Week N)" suffix on workout titles and a missing client-context line in the header. Both fixed via OpenCode lane (`lane-block-session-parity`, hand-reviewed diff), pushed to `staging` (`10d77cb`). Week-suffix removed at both live generation sources (`app/api/claude/generate-block/route.ts`, `lib/planGeneration.ts`) — a root-cause fix, not a display patch. Header now shows `{name} · {condition} · {N}-session block`, reusing the existing `clients.profile.health.conditions` field. `tsc --noEmit` clean. **Verified live on development 2026-08-19**: client-context line confirmed correct ("Emma Atkinson · Crohn's disease (10+ years, well managed) · 2-session block"). **Caveat found live: the "(Week 1)" suffix still shows on already-generated sessions** — the fix only changes label generation going forward, it doesn't retroactively clean `session.data.focus_label` values already written to the DB. A backfill would be needed for full resolution; not done. Not yet promoted to main. Full data-od-id pass (modals, schedule-form, edit-block-drawer) still not done — treat as first-pass, not exhaustive. |
| CR-EF-060 | Session/workout detail page (reached by clicking through from a block) has not implemented `hub-session.html` at all | CR (design parity) | **built, pushed to staging** | 2026-08-19 | Raised by Craig live against development.eternal-fitness.co.uk. **Claude's own live spot-check first**: the page was not "unbuilt" — major structure (mode-switch, progress bar, warm-up/main-block sections, superset/round structure) was present. Real bug found: header status pill and the exercise-logging progress label disagreed simultaneously ("Completed" + "IN PROGRESS" on screen at once). Root-caused via OpenCode lane: the pill correctly uses `deriveSessionStatus`/`SessionStatusPill` (untouched, protected per CR-EF-037); the progress-bar label was a *different* concept (exercise-completion progress) that happened to collide with the status vocabulary. Fixed by renaming that label "In progress" → "Partially logged" rather than forcing it into the shared status system. Pushed to `staging` (`10d77cb`), `tsc --noEmit` clean. Surfaced a new gap in the same investigation — see CR-EF-061. **Verified live on development 2026-08-19**: pill now reads "Completed" next to "7 of 8 exercises logged · PARTIALLY LOGGED" — no longer two disagreeing statuses. Not yet promoted to main; full data-od-id pass still outstanding. |
| CR-EF-061 | Session page's "Client intro" card renders the workout's own focus label instead of real client-specific intro copy — no genuine data source exists | CR (functionality/data) | raised | 2026-08-19 | Found investigating CR-EF-060. `session.data.client_intro` binding is correct — the bug is upstream: `client_intro` is populated at generation time with `parsed.client_intro ?? archetypeFocusLabels[slot.archetype]` (`lib/planGeneration.ts`, `app/api/claude/generate-block/route.ts`), and the AI prompt never asks for a real intro value, so the fallback always fires. Confirmed live rows hold `"client_intro": "Workout A"`. No existing schema field is a genuine "client intro" note — nearest candidates are `notes.esther_observations`/`notes.motivation_notes`/`notes.watch_for` on the client profile, or `coaching_notes` on the session, none of which fit. Needs a decision on what this field should actually contain (or a new field) before any fix — deliberately not patched blind. |
| CR-EF-062 | [BUG] Session page header doesn't match `hub-session.html` — wrong info architecture, not just cosmetic | Bug | **approved to build — off hold** | 2026-08-19 | Raised by Craig via screenshot annotation on the session page, circling the header through Warm-up. Initially held per Craig's instruction (no review until he says go); **re-raised with a direct mockup screenshot comparison, confirming it as a real gap** — off hold. Root cause confirmed by reading the mockup's render logic: title row should carry ONLY the focus_label + one status pill; live crams in an archetype badge ("B"), "Week N · Foundation" text, the status pill, AND a duration chip. Subtitle line should read `{client} · {condition} · {date} · {duration slot}`; live shows unrelated `{archetype name} · Session N` — because **the page never fetches the client record at all** (confirmed: only session/count/best-weights are loaded), so client name/condition/date literally aren't available to render. Dispatched to OpenCode lane (`lane-session-header-parity`) to add the missing client fetch and rebuild the header per the mockup's exact structure, reusing `client?.profile?.health?.conditions?.[0]` (already proven on the block page) and `clients.session_duration`/`formatSessionDuration` (already proven on the client detail page). The "(Week 1)" title suffix (CR-EF-059's retroactive gap) and "Client intro" card duplication (CR-EF-061) remain separately tracked — not part of this header-structure fix. **Built and pushed to staging** (`ef6ae8b`, hand-reviewed): title row now only focus_label + status pill; subtitle now reads `{client} · {condition} · {date} · {N} min slot` + the relocated duration-guide chip. Reused the existing `GET /api/clients/[id]` route (`select("*")` already returns `name`/`profile`/`session_duration`) — no new endpoint. Format calls: matched the mockup's "N min slot" wording over the `formatSessionDuration` helper's "1h" style, and the mockup's full-weekday date format — both explicitly flagged as judgment calls in the build summary. `tsc --noEmit` clean. **Verified live on development 2026-08-19**: title row is now "Workout A ... (Week 1)" + "Completed" pill only (no archetype badge, no "Week 1 · Foundation" text); subtitle reads "Emma Atkinson · Crohn's disease (10+ years, well managed) · Monday, 3 Aug 2026 · 60 min slot" + the guide chip — exact match to the mockup's format. (Title still carries the "(Week 1)" suffix — that's CR-EF-059's separate known retroactive-data gap, correctly untouched by this fix.) Not yet promoted to main. |
| CR-EF-064 | [BUG] Block page cards have inconsistent border-radius — schedule panel/meta-grid/week cards visibly sharper-cornered than Block Note and other `HubCard`-based cards on the same page | Bug | **built, pushed to staging** | 2026-08-19 | Raised by Craig via element inspector on the "Scheduled from..." panel. Root cause: `tailwind.config.ts` overrides `rounded-2xl` to **24px** in this project, while `HubCard` (`components/hub/HubCard.tsx`) uses an explicit `rounded-[16px]`. The block page's meta-grid strip, `BlockSchedulePanel.tsx`, and each week card all used `rounded-2xl`, producing a visibly different (larger) radius than the `HubCard`-based cards stacked right next to them. Mockup (`hub-block-module.html`) specifies 16px uniformly for `.card`/`.sched`/`.meta-grid`. **Claude fixed directly** (small, low-risk CSS-only change): all three switched to `rounded-[16px]`. `tsc --noEmit` clean. Pushed `staging` (`fca5dca`). **Verified live on development 2026-08-19** via computed-style check (`getComputedStyle`): meta-grid strip, schedule panel, and Block Note all now compute to a uniform 16px `border-top-left-radius`. Not yet promoted to main. Worth a follow-up sweep: other pages likely have the same `rounded-2xl`-vs-`rounded-[16px]` split (e.g. dialogs/modals on this same page still use `rounded-2xl` — left alone here since dialogs are a different UI pattern than stacked page cards, but flagging in case it's the same bug elsewhere). |
| CR-EF-065 | [BUG] `rounded-xl`/`rounded-2xl` Tailwind classes resolve to 32px/24px hub-wide, tuned for the marketing site, producing oversized/clipped-looking corners on hub cards, boxes, and controls | CR (root cause, umbrella) | **built and pushed to staging — full sweep complete** | 2026-08-19 | Raised by Craig live (voice) after CR-EF-064: he asked for a full border-radius audit "right across every face of the app," specifically noting the round-1/2/3 exercise boxes on the session-logging screen looked "cut off." Root-caused: `tailwind.config.ts` maps `xl: var(--radius-xl)` (32px) and `2xl: 24px`, where `--radius-xl`/`--radius-lg` (`app/globals.css`) were deliberately tuned across several commits for the marketing site's brand aesthetic — but Tailwind's config is global, so every hub component using `rounded-xl`/`rounded-2xl` inherited those values too, most visibly on small padded boxes (e.g. a round box with only 10px padding getting a 32px corner radius). Confirmed via grep: **93 files, ~500 occurrences** of `rounded-lg`/`rounded-xl`/`rounded-2xl` across `app/hub/**` + `components/hub/**`; the `--radius-xl`/`--radius-lg`/`--radius-md` tokens are otherwise unused anywhere else in the codebase (safe to leave in place, just shouldn't be wired into the shared Tailwind scale the hub also consumes). **Not safe to fix via the global Tailwind config** — that would also reshape marketing pages already built/verified against the current larger radii (CLAUDE.md documents rigorous design-parity work there). Correct fix is a scoped sweep through hub files replacing ambiguous shared classes with explicit pixel values matching the mockups (16px cards, ~12px nested boxes/controls, full pills) — same pattern already used for CR-EF-064's 3 fixes and this CR's part 1. **Part 1 built**: `SessionWorkoutLog.tsx` + the session page's own 2 remaining instances fully swept (8 instances corrected: round boxes, section wrappers, empty states, rest-timer widget, leg-split cards, completed-read-only banner, Client intro card). Pushed `staging` (`5a768d2`), `tsc --noEmit` clean. **Full sweep now complete**: 6 parts total across `wo-ef-hub-radius-sweep-2026-08-19`'s 5 lanes + the original manual fix — every `rounded-xl`/`rounded-2xl` instance in `app/hub/**` and `components/hub/**` replaced with explicit `rounded-[12px]`/`rounded-[16px]` values. Verified via `git grep` against the `staging` tip (`642254e`): **0 remaining occurrences**. All 5 lanes hand-reviewed before merge, `tsc --noEmit` clean on each. Commits: `5a768d2`, `bf0d448`, `6b13ab4`, `6161ef5`, `4cb7865`, `642254e`. Not yet verified live or promoted to main — needs a visual pass across a sample of screens, not just the grep confirmation. |
| CR-EF-066 | Client detail page — Overview tab has real, concrete gaps vs `hub-client-detail-refined.html` | CR (design parity) | **built + mockup reconciled** | 2026-08-19 | Full re-review at Craig's request. Findings: (1) Active Block card missing "Focus" field — **fixed**, added sourced from `blocks.block_note`. (2) Active Block/Last Session/Active Training Rules wrapped in accordions the mockup doesn't specify — Active Training Rules specifically is a safety concern (HARD constraints shouldn't need a click to reveal) — **fixed**, `defaultOpen` added. Pushed `staging` (`f1af11c`), `tsc` clean, not yet verified live. (3) Two extra real cards ("Snapshot", "Training snapshot") + (4) alert's bulleted-list content shape — both legitimate live functionality the mockup predated — **mockup updated** (`hub-client-detail-refined.html`, Open Design) to add both cards and the real alert shape, using values consistent with the file's existing Marlene Thatcher persona. |
| CR-EF-067 | Client detail page — Profile tab has real, concrete gaps vs `hub-client-detail-refined.html` | CR (design parity) | **mockup reconciled (partial)** | 2026-08-19 | Mockup had 6 sections; live has 8 — adds "Client Portal" and "Record". **Mockup updated** (Open Design) to add both, using real observed content (portal-account-not-yet-created state + actions; client number/created/last-edited). **Health section fields left untouched** — mockup shows Age/Referral route/source/reason (plus, on closer full read, also Declared conditions/Current medications span-full fields I'd initially missed); live shows GP Clearance/Conditions (tags)/Relevant Medications (tags), no Age/Referral at all. This is an edit to *existing* mockup content, not an addition — deliberately not touched blind; still needs Craig's confirmation on which fields Esther actually wants tracked before either side changes. |
| CR-EF-068 | Client detail page — right rail has a "Resources" card not in `hub-client-detail-refined.html` (mockup rail is Status + Active Block only) | CR (design parity) | **mockup reconciled** | 2026-08-19 | Live rail shows Status, Active Block, and Resources (portal-access toggles). **Mockup updated** (Open Design) to add a third rail card using the real observed content ("Your daily calorie guide"/"Showdown Soundboard", both Disabled). |
| CR-EF-063 | [BUG] Block page session rows implement an entirely different interaction model than `hub-block-module.html` — inline exercise table + extra row icons the mockup never designed | CR (design parity) | approved to build | 2026-08-19 | Found after Craig flagged (5th time) that CR-EF-059's earlier "close to the mockup" verdict was wrong — that pass only confirmed page *sections* existed, never read the mockup's own `sessionHtml()`/`actionsHtml()` JS to check row *content*. Actual mockup behaviour: session rows are collapsed one-liners (day+time · workout-type pill · name · status pill · 1-2 action buttons) that link to `hub-session.html` for all exercise detail — `.sess-expand`/`.ex-table` CSS classes exist in the file but are **dead, never rendered** by any JS path. Action set per status: completed → outline "View" only; cancelled → ghost "View" only; planned → outline "Edit"; scheduled/in-progress → teal "Log"/"Resume" + ghost "Reschedule". No copy icon, no delete icon, no separate "Edit session" button, no inline expand anywhere. Live app instead expands every row into a full sets/reps/tempo/rest table in place, plus copy/delete/chevron icons and a separate "Edit session" text button not in the mockup's action set at all. Also: mockup's day cell includes time ("Mon 3 Aug · 10:00"), live shows date only. **Craig confirmed 2026-08-19: rebuild to match the mockup exactly** (not "keep as an enhancement") — approved to build. **Built and pushed to staging** (`8bd8d0d`) via OpenCode lane (`lane-block-rows-parity`, hand-reviewed diff): inline exercise table + per-row clone/delete/edit-session icons replaced with a new `SessionRow.tsx` collapsed one-liner matching `sessionHtml()`/`actionsHtml()` exactly, status-driven action buttons, day+time cell. Clone/delete API routes untouched, only the row-level UI trigger removed (per explicit approval). Reschedule reuses the existing `PATCH /api/sessions/[id]` pattern. `tsc --noEmit` clean. **Verified live on development 2026-08-19**: rows render as clean one-liners exactly matching the mockup (day+time, archetype pill, name, status pill, correct action set per status — Completed→View, Scheduled→Log+Reschedule); inline reschedule picker confirmed working (opens pre-filled, Save/Cancel present). Not yet promoted to main. |
| CR-EF-071 | Client-detail tab badge appears on the wrong tab — mockup shows it fixed to Admin, live has it on Comms for the client Craig was checking | CR (design parity) | **mockup reconciled — not a bug** | 2026-08-19 | Raised by Craig live (production, client #8) via element inspector on the tab strip, comparing to the mockup's Admin badge. Traced in code (`app/hub/(protected)/clients/[id]/page.tsx:421,428`, `TabCountBadge`): the badge is **independent and data-driven per tab, not fixed to one tab** — Admin's badge = outstanding compliance actions for that client, Comms' badge = pending tasks for that client. Emma Atkinson (client #8) currently has 0 outstanding admin items and 3 pending tasks, so only Comms shows a badge; Marlene Thatcher (the mockup's persona) has the reverse (1 admin item, 0 tasks), so the mockup only ever showed Admin's. **This directly contradicts CR-EF-045's 2026-08-18 conclusion** ("badge moved Comms→Admin to match the live app") — that fix was based on incomplete sample data that never exercised both tabs having a count at once, not a real single-location design. **Mockup updated** (Open Design, `hub-client-detail-refined.html`) with an authoring comment documenting both tabs can independently badge; Marlene's sample Admin badge left as-is (still valid for her data) since adding a fabricated Comms count for her would just trade one incomplete sample for another. No code change — live behaviour is correct. |
| CR-EF-073 | Session/block naming doesn't follow through across surfaces — same session appears as "Session 3", "S3", "B2 S3", "Block 2 · S3", "Workout A", and "Day 2" depending on where you look | CR (naming convention, cross-surface) | **built, pushed to staging — verified live** | 2026-08-20 | Raised by Craig (voice, 2026-08-20); v2 proposal (block = dated period, session = booked calendar slot, workout = attached on the day) confirmed 2026-08-21. Full proposal: `.context/proposal-naming-convention-2026-08-20.md`. **Mockups updated first** (`hub-block-module.html`: header date span, session-row ordinal + "not yet booked", next-session cell, session-edit-entry subtitle; `hub-client-detail-refined.html`: "Started"→"Dates" on both Active Block cards + Training Blocks table). **Built**: block date span everywhere derived from real session `scheduled_at` (never fabricated, "Not yet scheduled" fallback) — client Overview/right-rail Active Block cards, Training Blocks table, block page header. Session rows lead with date+time; unbooked sessions read "Session N of M · not yet booked" (replaces "Day N"). Block-page Next-session stat and Sessions-tab/dashboard labels spelled out ("Session 3", "Block 2 · Session 3" — no more "S3"/"B2 S3"). Exercise-trend chart keeps its short axis tick but tooltip now uses a new `fullLabel` field spelling the point out in full. `tsc --noEmit` clean. Pushed `staging` (`71eacf5`) after merging in `cr-ef-076-078-client-detail-polish`'s already-landed restructure (auto-merged clean, one trivial `tsconfig.tsbuildinfo` conflict). **Verified live on development.eternal-fitness.co.uk 2026-08-21** (client #8, Emma Atkinson): Active Block cards and Training Blocks table show "3 Aug 2026 – 5 Aug 2026"; Sessions tab shows "Block 1 · Session 2"; block page header shows the block's date span, Next session reads "Wed 5 Aug · Session 2 of 2", session rows show "Session 1 of 2"/"Session 2 of 2"; session detail page subtitle reads "...· 60 min slot · Session 2 of 2"; Progress tab chart renders with no console errors. Not yet promoted to `main`. |
| CR-EF-072 | [BUG] Client-detail tab strip rendered as a filled rounded-pill panel (`HubTabsList`/`HubTabsTrigger`), not the mockup's bare underline tabs | CR (design parity) | **built, pushed to staging — verified live** | 2026-08-19 | Found immediately after CR-EF-071 — Craig clarified his real point was the tab *strip's container style*, not the badge. `HubTabsList`'s own code comment referenced an old mockup filename (`hub-client-detail.html`, not the current `hub-client-detail-refined.html`) — built against a stale spec and never updated. Mockup's `.hub-tabs`/`.hub-tab`: flex row, single bottom border under the whole strip, transparent triggers, active tab = rose text + rose bottom-underline, no background/radius/shadow. Live had: `rounded-[12px] border ... bg-[var(--hub-card)] shadow-sm` panel with `data-[state=active]:bg-[var(--hub-sidebar-active)]` filled-pill active state — a different UI pattern entirely. **Confirmed shared-component scope before building** (only one current usage, `clients/[id]/page.tsx`, but it's the designated canonical component per CR-EF-039's "one tab component" standardization) — Craig approved fixing hub-wide. Rewrote `components/hub/HubTabs.tsx` to match the mockup's underline-tab spec exactly. `tsc --noEmit` clean. Pushed `staging` (`897386b`). **Verified live on development 2026-08-19**: tab strip now renders as bare underline tabs, active tab (Overview) shows rose text + rose underline, no panel background. Not yet promoted to main. |
| CR-EF-073 | Client-detail information architecture — "Admin" tab renamed **Compliance**, Documents promoted to its own tab, and Overview cut back to top-level only | CR (design parity) | raised | 2026-08-20 | Raised by Craig in an Open Design review of `hub-client-detail-refined.html`. Three changes, one restructure. (1) **Admin → Compliance**: the tab's badge already counts outstanding *compliance* actions and `ClientDetailTabs.tsx:20` already carries a `compliance → admin` alias, so the label was the only thing out of step; alias flips to `admin → compliance` so existing links and bookmarks keep resolving. (2) **Documents gets its own tab**: `DocumentRegister` is a working surface with version/last-updated columns, per-row actions, create-and-upload flows and legacy read-only rows — it was the largest card on a tab it didn't belong to. (3) **Overview reduced to current state**: keeps the outstanding-actions alert, Active block, Last session, Active training rules and the Notes panel; "Snapshot" moves to Profile, "Training snapshot" moves to `TrainingTabContent`, and `UpdateIntervalControl` moves to `CommsTabContent` beside the updates it schedules. **Decided 2026-08-20**: `PackagePaymentsCard` stays on the renamed Compliance tab despite being the odd one out — it may be removed entirely at some point, so not worth moving twice. Mockup is the spec and is already updated. Files: `ClientDetailTabs.tsx`, `clients/[id]/page.tsx` (only 4 references to the `admin` tab exist repo-wide, both files). |
| CR-EF-074 | Plan Agent user chat bubble is solid rose with white text — 3.0:1 on a paragraph of body copy | CR (accessibility) | raised | 2026-08-20 | Found while designing the Plan Agent tab, which had never been designed at all (its mockup panel read "CONCEPT — not yet designed"). `PlanAgentTab.tsx` paints the user's message bubble `bg-rose text-white`. Rose `#C1839F` against white computes to **3.0:1** — acceptable for a short button label, below the 4.5:1 needed for a block of running text. **Decided 2026-08-20**: adopt the mockup's treatment — rose-tinted surface (`color-mix` 14% rose into white) with ink text and a 32% rose border. Same "this one is mine" read, full contrast, and it still pairs visually with the neutral assistant bubble. Does not touch the rose *buttons*, which stay as they are — a label at 3.0:1 is a separate question from a paragraph. File: `PlanAgentTab.tsx`. |
| CR-EF-075 | Document templates library — no way to assign a template to a client without opening the template first | CR (functionality) | raised | 2026-08-20 | Raised by Craig: "there are other routes as well such as when you first assign the template to the client, same as all templates." Two shared routes create a client document — from the client (`NewDocumentButton`, correct already) and from the template (`SendTemplateToClient` on `/hub/templates/[id]`). The library grid at `TemplatesLibrary.tsx` offers only *Open* and *Preview* per card, so the second route costs an extra hop through the template detail page. Add *Assign to client* per card, opening the same client-picker → create-draft → land-on-client-document flow. **Must stay a shortcut into the existing flow, not a reimplementation**: `SendTemplateToClient`'s own comment records that an earlier version faked its own "sent" status with no email behind it, which is how documents showed as sent when nothing had gone out. Sending/resending/copy-link stay on the document page. File: `TemplatesLibrary.tsx`. |
| CR-EF-076 | Overview's outstanding-actions alert always shows the full action list — mockup makes it collapsible | CR (design parity) | **built, pushed to staging** | 2026-08-20 | Mockup (Open Design, `hub-client-detail-refined.html`): the alert is a native `<details>`/`<summary>` accordion, closed by default, header shows the count + one-line summary, list expands on click. `HubAlert` gained opt-in `collapsible`/`defaultOpen`/`summary` props — every other usage (Do Not Train banner, gone-quiet banner, pending_medical text) is untouched, still always-expanded. The Overview `action_needed` banner now uses them. Rebased onto CR-EF-073's concurrent Admin→Compliance rename same day; summary copy updated to "see Compliance tab" to match. `tsc --noEmit` clean. Not yet verified live — dev server here can't reach the DB (no SSH tunnel in this session). File: `HubAlert.tsx`, `clients/[id]/page.tsx`. |
| CR-EF-077 | Client-detail page — Tasks has no persistent entry point, two clicks deep under Comms → Updates with no visual cue it exists | CR (usability) | **built, pushed to staging** | 2026-08-20 | Mockup (Open Design): new **Tasks** quick action in the qa-bar next to Plan Block, badged with the open-task count, jumping straight to Comms → Tasks. `HubQuickActions` gained an optional `badgeCount` prop on the bar variant, reusing the `pendingTaskCount` already computed for the Comms tab's own badge — no new query. Links to the existing `?tab=comms&view=tasks` deep link (already supported by `ClientDetailTabs`/`CommsTabContent`, confirmed before wiring). `tsc --noEmit` clean. Not yet verified live (same DB-tunnel gap as CR-EF-076). File: `HubQuickActions.tsx`, `clients/[id]/page.tsx`. |
| CR-EF-078 | Profile tab's collapsible sections read as flat/lost against the canvas; mockup asked for an icon colour accent | CR (design-consistency) | **built, pushed to staging** | 2026-08-20 | Investigated: the "flat, no background" complaint was a mockup-only gap (the HTML mockup's section class had no shadow) — live `HubCard` already ships `shadow-sm`, so no live change was needed for that part. The colour-accent ask was applied to the **Snapshot** section specifically (matches the mockup's own target by name) — recoloured `color="navy"` → `color="teal"` (`#087E8B`, same token as the mockup's accent-secondary). Note: this CR was drafted before CR-EF-073 (same day) moved Snapshot from Overview to Profile; originally applied to Health as Snapshot's closest live equivalent, corrected to the real Snapshot section once rebased onto CR-EF-073. Health left at its original `color="neutral"`. `tsc --noEmit` clean. Not yet verified live. File: `clients/[id]/page.tsx`. |

## CR-EF-016 – CR-EF-027 — raised by Craig, 2026-08-17

A single hub/PWA usability pass raised verbally in chat and captured here the same
day. Every row above was checked against the shipped code before being written —
the "Verified:" notes are file/line reads, not assumptions.

**All three open questions answered by Craig the same day** (`wo answer`
`qmsxbh95t0x` / `qmsxbh96xbu` / `qmsxbh982vf`): CR-EF-018 is always-on for a
known unilateral list, CR-EF-024 is EF-local tasks only, and CR-EF-025 is closed
as not wanted. Nothing in this batch is gated on a decision any more — eleven
live items, one closed.

**Register-accuracy note, 2026-08-17.** CR-EF-024 as first written proposed
building a client task system. Craig corrected it the same day: the hub tasks
module already exists and is already on the desktop client page. The row now
reflects the real, much smaller gap (the PWA client profile). Worth carrying
forward as a caution for this register: an item phrased as a feature request
("quick action task on client page") can be a *placement* gap on one surface
rather than a missing capability — check both the desktop and PWA surfaces
before sizing anything in this batch. CR-EF-025 turned out the same way (the
column already existed).

**Clustering for execution** — these should not become twelve separate pieces of work:

- **Rides CR-EF-011's workout-surface consolidation** (same screens, same mockups
  still pending): CR-EF-018, CR-EF-019, CR-EF-020, CR-EF-021. Building any of
  them before those Open Design briefs exist risks work that the redesign then
  throws away — the same trap already flagged on CR-EF-015.
- **Client profile / record shape** (schema + both profile surfaces):
  CR-EF-017, CR-EF-022, CR-EF-024. Now three items, not four — CR-EF-025 is
  closed. CR-EF-017 (notes) and CR-EF-024 (tasks) share a surface and an intent
  and should be designed as one pass.
- **Independent, small, shippable now**: CR-EF-016, CR-EF-027, and CR-EF-023
  (self-contained in the updates-due module).
- **Bug, own path**: CR-EF-026 — diagnose from data before writing any code.

## Parked integrations (mirrored in FF-001 §M12, held in the ops registry)

Reconciled into the tracked file 2026-08-17 from the same never-committed series.

**INT-001 — Outlook calendar integration.** Built 2026-08-15 (L6, real OAuth) by
a concurrent session without a CR at the time. Merged to main+staging 2026-08-17
alongside this session's design-parity/monthly-calendar work — production live,
not just staging. FF-001 §M12 needs confirming it reflects this.

**INT-002 — Native Bookings picker.** Custom Microsoft Bookings date/time picker
via Graph API, replacing the iframe modal; needs Azure AD app registration +
admin consent (registry deferred `dmsk355ygro`). Still unscoped as its own Work
Order.

## Decisions — 2026-08-17 (later), Craig

Cleared in one sitting with Craig ("yes to all", then "items 3 deferred until
further notice"):

- **CR-EF-011 consolidation — unblocked.** G1 (naming story) and G3 (desktop
  Session Editor keeps its inline logger — the single desktop logging surface)
  approved. G2 mockups to verify against the Open Design briefs already in
  `D:\apps\design-systems\ef-control-hub`. L3/L4/L6 were already built by
  concurrent sessions (`62026f4` desktop consolidation, `22ff6c9`
  templates-browser + paste-and-assign, `1a07d34` portal PWA — `portal.webmanifest`
  + `portal/sw.js` confirmed present, superseding the stale "portal PWA not built"
  note on CR-EF-012).
- **Approved to build — BUILT 2026-08-17:** CR-EF-019 (rest-timer sound + vibrate),
  CR-EF-020 (in-session rest-time override), CR-EF-024 (PWA client-profile tasks
  panel, reusing the existing tasks module).
- **Approved to build — still pending:** CR-EF-021 (per-exercise edit — rides the
  consolidation mockups, not yet signed off), CR-EF-017 (client notes — schema +
  design brief), CR-EF-022 (start date — schema + intake + backfill source).
- **Deferred until further notice:** blog repositioning migration (scope-of-works
  §2.5), hub nav restructure (§2.6), GDPR DPA signature + ICO registration (§2.2),
  CR-EF-014 (band colour→tension — needs Esther), CR-EF-018 (unilateral L/R curated
  list — needs Esther).
- **Still needs Craig (manual, not deferred):** §1.1 WAL/PITR archive gap, §1.2
  staging shares live email creds, §4.2/§4.4 branch & worktree cleanup.

## Outstanding process gap — RESOLVED 2026-08-17

The prior note here ("no confirmed live `project_id`") was wrong — there is a live,
active project in `decoded-ops-hub` (`Eternal Fitness — Website & Tooling`,
`project_id 33ccfd11-0a76-4170-afc4-491a51d899c2`) with a real `tasks` board that
had simply drifted out of sync with this file. Reconciled 2026-08-17 (Craig-requested
board review):

- Deleted 2 exact-duplicate "production go-live cutover" tasks (kept the one with
  real notes), deduped a repeated WAL/PITR `deferred_items` entry.
- Reopened 3 tasks wrongly marked `completed` (`/exercise-for-health` hub index,
  `/high-blood-pressure`, `/visual-impairment`) — the content pass happened, but the
  routes are currently redirected again per the same-day re-gate (see CR-EF-003
  note above), so "done" didn't match live reality.
- Closed 2 stale backlog tasks whose underlying work was actually finished weeks
  ago (£45 single-session tier removal, "Level 4 Personal Trainer" claim retirement).
- Seeded hub tasks for every currently-open/approved CR that had no board
  counterpart: CR-EF-006, CR-EF-008, CR-EF-011, CR-EF-012 (`in-progress`), CR-EF-014,
  CR-EF-028 (originally raised as CR-EF-016, see the ID-collision note below) —
  each tagged with its CR ID in `task_ref` so the two systems can be
  cross-checked going forward.

**ID collision, caught at session close 2026-08-17.** This CR was originally
raised as CR-EF-016 (Outlook calendar confirm-before-sync gate) earlier the
same day. A concurrent session raised its own CR-EF-016–027 batch (client-list
sort, rest-timer sound, etc. — see the table above) without either session
seeing the other's in-flight edit, and that batch's CR-EF-016 (client list
A–Z) overwrote this row entirely when both were merged. Recovered from this
session's own notes and re-added below as **CR-EF-028** — content unchanged,
number only. Flagging as a live process gap: concurrent sessions editing the
same git-tracked register can silently clobber each other's rows on merge,
same failure shape as the original pre-git-tracking collision this file's
history section already documents. No caught-in-time safeguard exists yet
beyond careful conflict resolution at merge time.

There's also a second, unrelated `decoded-ops-hub` project — `Eternal Fitness —
Accessible Training Platform` (archived, 17 backlog tasks, created 2026-07-15) — an
earlier multi-tenant/white-label product concept, distinct from the actual build.
It's archived so it doesn't surface on the active board, but flagging it here in
case anyone finds it and assumes it's live scope.

CR-EF-002 (contact-form spam) is already `built` in this register — no hub bug
task needed for it retroactively.

**Register-history gap — RESOLVED 2026-08-17.** An earlier CR-EF-001 through
CR-EF-005 series (workout-surface consolidation, PWA installability, a stale
webmanifest string, band-colour prescription, and a retro-captured
workout-from-template feature) existed only in the shared checkout's working
tree (`D:\apps\eternal-fitness-website\.context\change-requests.md`), never
git-committed — invisible to the SEO-audit session that bootstrapped this
file fresh and collided on IDs (not content). Reconciled into this file as
CR-EF-011 through CR-EF-015 plus the parked-integrations section, same day.
The stale webmanifest string (CR-EF-013) was fixed in the same pass. The
shared checkout's uncommitted copy is now safe to discard — everything in it
has a home here.

**Register-fork, third incident — RECOVERED 2026-08-18.** Same failure shape
as the two incidents above, one register generation later. CR-EF-029–041 were
raised 2026-08-17 (evening/night) by a session working on branch
`claude/blocks-sessions-workouts-mobile-64d18f`, which committed them to its
own copy of this file and to an untracked
`.context/workorder-ef-hub-structure-consistency-2026-08-17.md` — but never
merged into `main`. `main`'s own `change-requests.md` silently stopped at
CR-EF-028 while three commits (`404566b`, `c44fa0c`, `7e55084`) and this
project's own briefs went on citing CR-EF-038/039/040 as if they were live
rows here. Recovered 2026-08-18 by diffing that branch's register against
this one and splicing in the 13 missing rows verbatim (content unchanged,
same numbering — no collision this time, just absence). The branch itself
(1 commit ahead, 29 behind `main`) is a documentation-only close-out and is
being folded in separately.

**Six rows corrected from `raised` to `built`, 2026-08-18.** CR-EF-017, 019,
020, 021, 022 and 024 were marked `raised` in this table while the prose in
the "Decisions — 2026-08-17 (later), Craig" section above already said
"BUILT 2026-08-17" for three of them — the table and the narrative had
drifted apart inside the same file. Verified each against the commits that
actually deliver it (`cb2d5ed`, `4191575`) before flipping the status; see
each row's note for the specific evidence.

**No CR in this register has ever reached `verified`.** `built` is the
highest status anything has attained (CR-EF-041 reads "Open Design pass
built + verified" but that describes one lane of four, not the whole CR —
Lane D is explicitly still outstanding in its own note). Either the status
gets used or it should be dropped from the stated flow; a status nothing
reaches is decoration, not a control.

**Register-fork, fourth incident — RECOVERED 2026-08-19 (evening).** Same
failure shape as the three incidents above, discovered only because this
session held its own running edits to this file in a local, never-pushed
worktree (`claude/page-design-issues-80a514`) all day while a concurrent
session built and pushed CR-EF-047 (Docker build-cache mount, rejected)
and CR-EF-048 (endurance-block document kind, built + live-verified,
`7ee105f`/`f35780b`/`32718c4`) directly to `staging` — neither of which
this session's local copy had. Meanwhile this session had independently
minted its own CR-EF-048/049 for the dashboard alert-accordion and
Recent-blocks fixes. Recovered by diffing this session's local file
against `origin/staging`'s real one: kept the genuine CR-EF-047/048
untouched, renumbered this session's colliding pair to **CR-EF-069/070**
(content unchanged, the `workorder-ef-hub-dashboard-client-training-
parity-2026-08-19.md` WO doc's references updated to match). No other IDs
in this session's 050–068 range collided. Flagging again: this is the
fourth time concurrent edits to this one file have silently clobbered or
duplicated each other — a real, recurring process gap, not a one-off.
